import type { CodegenIR, GeneratedFile } from '../../types';
/**
 * Emits a route map interface that maps route keys (e.g., 'GET /users/:id')
 * to their input/output types for type-safe test app usage.
 */
export declare function emitRouteMapType(ir: CodegenIR): GeneratedFile;
//# sourceMappingURL=emit-routes.d.ts.map
