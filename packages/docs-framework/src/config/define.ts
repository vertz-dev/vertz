import type { DocsConfig } from './types';

/**
 * Define a documentation site configuration.
 * Identity function that provides type-checking and editor autocompletion.
 */
export function defineDocsConfig(config: DocsConfig): DocsConfig {
  return config;
}
