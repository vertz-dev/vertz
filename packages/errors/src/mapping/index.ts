/**
 * Error mapping utilities.
 *
 * Functions for mapping between different error representations
 * across layers (DB → HTTP → Client).
 */

export * from './db-to-http.js';
export * from './http-to-client.js';
