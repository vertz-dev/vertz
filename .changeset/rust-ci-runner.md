---
'@vertz/ci': patch
---

Add `@vertz/ci` TypeScript SDK for defining CI pipelines with `pipe()`, `task()`, and `cond.*` builders. Includes NDJSON loader bridge for the `vtz ci` Rust binary, discriminated union types for tasks/conditions/deps, and callback interception for programmatic dependency control.
