---
'@vertz/docs': patch
'@vertz/cli': patch
---

Add `vertz docs check` command to validate docs site integrity — catches broken sidebar references, broken internal links, and warns about missing frontmatter.
