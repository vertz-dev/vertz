---
'@vertz/server': patch
---

Add cloud-managed auth infrastructure: JWKS client, RS256 JWT verifier, auth proxy with circuit breaker, provider cloud config union types, lifecycle callbacks, and SSR cloud verifier support.
