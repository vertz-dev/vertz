{
  "name": "vertz-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "test": "turbo run test --filter='!@vertz-examples/*' --filter='!entity-todo-example' --filter='!task-api-example'",
    "test:watch": "bun run --filter '*' test:watch",
    "lint": "biome check packages/",
    "lint:fix": "biome check --fix packages/",
    "format": "biome format packages/",
    "format:fix": "biome format --fix packages/",
    "ci": "turbo run lint build typecheck test --filter='!@vertz-examples/*' --filter='!entity-todo-example' --filter='!task-api-example'",
    "ci:affected": "turbo run lint build typecheck test --filter=...[origin/main] --filter='!@vertz-examples/*' --filter='!entity-todo-example' --filter='!task-api-example'",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "bun run build && changeset publish",
    "postinstall": "if [ \"$(git rev-parse --git-dir 2>/dev/null)\" = \"$(git rev-parse --git-common-dir 2>/dev/null)\" ]; then lefthook install || true; fi",
    "test:tree-shaking": "npx vitest run --config tests/tree-shaking/vitest.config.ts --root tests/tree-shaking",
    "build": "turbo run build --filter='!@vertz-examples/*' --filter='!entity-todo-example' --filter='!task-api-example'"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.7.0",
    "happy-dom": "^18.0.1",
    "@ampproject/remapping": "^2.3.0",
    "@biomejs/biome": "^2.3.14",
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.29.8",
    "esbuild": "^0.27.3",
    "jiti": "^2.4.2",
    "lefthook": "^2.1.1",
    "magic-string": "^0.30.21",
    "ts-morph": "^27.0.2",
    "turbo": "^2.8.8",
    "vitest": "^4.0.18"
  }
}
