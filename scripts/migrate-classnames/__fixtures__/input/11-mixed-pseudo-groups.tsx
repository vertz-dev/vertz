import { css } from '@vertz/ui';

export const styles = css({
  link: [
    'text:primary',
    'hover:text:primary.700',
    'hover:bg:primary.100',
    'focus:outline-none',
    'active:opacity:0.8',
  ],
});
