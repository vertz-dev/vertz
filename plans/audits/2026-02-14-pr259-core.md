# Audit: PR #259 "feat/ui 033 compiler diagnostics"
**Date:** 2026-02-14 | **Agent:** vertz-dev-core[bot] + vertz-tech-lead[bot] | **PR:** #259 | **Grade:** D

## Executive Summary

PR #259 suffers from a **critical scope violation**: the PR title claims to implement "feat/ui 033 compiler diagnostics" (ticket ui-033), but the actual changes are:
1. Infrastructure fix for lefthook pre-push hooks (infra-002) — unrelated to ui-033
2. JSX conditional branch bug fix — related to compilation, but NOT the diagnostic rules from ui-033

The ticket ui-033 requires implementing 5 new diagnostic rules (`lifecycle-scope`, `untransformed-jsx`, `missing-cleanup`, `raw-dom-mutation`, `signal-outside-component`). **None of these were implemented in this PR.** The actual diagnostic implementation (commit d800177) exists on a separate branch and was never merged.

This is a misleading PR that merges unrelated work under a false feature flag, violating scope discipline and design compliance.

---

## TDD: ⚠️ (Cannot Verify)

**Finding:** Without session transcripts, I cannot verify the test-first order. However, the PR includes tests for both changes:
- `lefthook-config.test.ts` (68 lines) for the infra fix
- `jsx-conditional-nested.test.ts` (120 lines) for the JSX bug fix

**Evidence from commits:**
- Commit `fc14d12` (infra fix): Test file created alongside implementation
- Commit `1d425f6` (JSX fix): Test file created, references "Bug #255" in test descriptions

**Issue:** Test descriptions reference "Bug #255" which was a PREVIOUS PR. This suggests the bug fix was retrofitted after discovering a production issue, rather than following TDD from a ticket.

**Test Quality:**
- ✅ Tests are comprehensive with multiple edge cases
- ✅ Tests verify actual behavior, not implementation details
- ✅ Test names clearly describe what they're testing

**Unknown (no transcript):**
- ❓ Were tests written BEFORE implementation?
- ❓ Was there a red-green cycle?
- ❓ Were quality gates run before commits?

**Severity:** Minor — tests exist and are high quality, but order cannot be verified

---

## Process: ❌ (Multiple Major Violations)

### Violation 1: Missing Changeset (Major)
**Evidence:** PR modifies three packages but includes NO changeset:
- `packages/integration-tests/package.json` — added `yaml` dependency
- `packages/ui-compiler/src/transformers/jsx-transformer.ts` — bug fix
- `packages/ui/src/dom/conditional.ts` — bug fix

**Rule:** RULES.md states "Changeset required for any package change"

**Impact:** Version bumps and changelog entries won't be generated for these changes

**Recommendation:** Add changeset with type `patch` for bug fixes in `@vertz/ui-compiler` and `@vertz/ui`

### Violation 2: Branch and PR Mismatch (Critical)
**Evidence:**
- Branch name: `feat/ui-033-compiler-diagnostics`
- PR title: "feat/ui 033 compiler diagnostics"
- Ticket ui-033 scope: Implement 5 diagnostic rules
- **Actual PR contents:**
  - Commit fc14d12: `fix(infra): add quality-gates command to pre-push hook (infra-002)` ← Different ticket!
  - Commit 1d425f6: `fix(ui-compiler): transform JSX in conditional branches` ← No ticket reference

**Rule:** RULES.md states "Never commit without a ticket" and "Design deviations: Stop. Do not silently deviate."

**Impact:** 
- Misleading PR title — other developers expect diagnostic rules but get unrelated fixes
- Ticket ui-033 status is incorrect (still shows Todo, but PR is marked as completing it)
- Scope creep — two unrelated fixes bundled into one PR

**Recommendation:**
1. PR #259 should have been TWO separate PRs:
   - PR for infra-002 (lefthook fix) from `fix/infra-002-prepush-hook` branch
   - PR for JSX bug fix from a dedicated bug fix branch
2. Create a NEW PR for actual ui-033 implementation (diagnostic rules commit d800177)
3. Update ticket ui-033 status to reflect that it's NOT complete

### Violation 3: Commit Convention Issues (Minor)
**Evidence:** 
- First commit: `fix(infra): ...` uses `infra` as scope, but changes `lefthook.yml` (repo root) and `packages/integration-tests`
- Second commit: References "Bug #255" in commit message body, which was a previous PR merge

**Rule:** RULES.md "Scope by package: `feat(db):`, `fix(ui-compiler):`"

**Recommendation:** Multi-package changes should either:
- Be split into separate commits per package, OR
- Use a more general scope like `fix(repo):` for root-level changes

---

## Design: ❌ (Critical Scope Violation)

### Violation: PR Does Not Match Ticket Scope (Critical)
**Evidence:**

**Ticket ui-033 requirements:**
```
Implement 5 diagnostic rules:
1. lifecycle-scope (error)
2. untransformed-jsx (warning)
3. missing-cleanup (warning)
4. raw-dom-mutation (warning)
5. signal-outside-component (error)

Files to create:
- packages/ui-compiler/src/diagnostics/lifecycle-diagnostics.ts
- packages/ui-compiler/src/diagnostics/jsx-diagnostics.ts
- packages/ui-compiler/src/diagnostics/cleanup-diagnostics.ts
- packages/ui-compiler/src/diagnostics/dom-mutation-diagnostics.ts
- packages/ui-compiler/src/diagnostics/signal-diagnostics.ts
```

**PR #259 actual changes:**
- ❌ NO diagnostic rule files created
- ❌ NO new diagnostics integrated into compiler pipeline
- ✅ One lefthook config fix (unrelated)
- ✅ One JSX transformer bug fix (unrelated)

**Git history shows:**
- Commit d800177 "feat(ui-compiler): Add 5 diagnostic rules" EXISTS but is on a separate branch `pr-258`
- This commit was NOT included in PR #259 merge commit (1795647)
- The diagnostic rules have never been merged to main

**Rule Violations:**
- RULES.md: "Never refactor unrelated code. Stay in scope."
- RULES.md: "Don't skip reading the ticket and design doc. The answer to 'what should I build?' is in the ticket"
- auditor.md: "Files modified match ticket scope (no scope creep)"

**Impact:**
- **Customers/stakeholders expect diagnostic rules** based on PR title, but they don't exist in production
- Ticket ui-033 is marked as complete when it's actually not started
- Unrelated bug fixes are hidden under a misleading feature flag
- Diagnostic work (d800177) is orphaned on a side branch

**Recommendation:**
1. **Revert the PR #259 title and close ticket ui-033** — it was never completed
2. Create proper PRs for the actual changes:
   - `fix(infra): lefthook quality gates` from infra-002 branch
   - `fix(ui-compiler): conditional JSX transform` from a bug-fix branch
3. Create a NEW PR that actually implements ui-033 using commit d800177
4. **Investigate:** Why did the bot merge unrelated work under a feature ticket? Session transcript would reveal the decision process.

---

## DX & Quality: ⚠️ (Mixed)

### Positive:
- ✅ Tests are comprehensive and well-written
- ✅ Bug fix includes detailed explanation in commit message (3 specific bugs identified)
- ✅ Lefthook fix includes rationale ("No longer requires LEFTHOOK=0 workaround")

### Negative:
- ❌ No Developer Walkthrough for the "feature" (though it's actually bug fixes, not a feature)
- ❌ No examples demonstrating the fixes
- ❌ No documentation update explaining the lefthook changes

**Rule:** RULES.md "Developer Walkthrough included for features"

**Mitigation:** Since these are bug fixes (not features), the Developer Walkthrough requirement may not apply. However, the PR title claims it's a feature (`feat/ui-033`), which creates confusion.

---

## Security: ✅ (No Issues)

**Evidence:** Code review shows:
- ✅ No `eval()` or `new Function()`
- ✅ No hardcoded secrets
- ✅ No unsafe DOM manipulation patterns
- ✅ Input sanitization not needed (test infrastructure changes only)

All security checks pass.

---

## Git & PR Workflow: ⚠️ (Partial Compliance)

### Positive:
- ✅ Used bot scripts: `vertz-tech-lead[bot]` and `vertz-dev-core[bot]` Git signatures
- ✅ Did NOT push to main directly (went through PR)
- ✅ Commits are atomic with clear messages
- ✅ Conventional commit format followed

### Negative:
- ❌ Branch name (`feat/ui-033-compiler-diagnostics`) does not match actual work
- ❌ Two different bots contributed to the same "feature" branch (tech-lead for infra, core for compiler fix)
- ⚠️ PR description is minimal (just two bullet points), doesn't explain why these changes are bundled together

**Rule:** RULES.md "One feature branch per design — phase PRs target the feature branch"

**Issue:** This feels like two independent fixes that accidentally got pushed to the wrong branch.

---

## Overall Assessment

**Grade: D** — Multiple major violations, critical scope mismatch

### Critical Issues (Must Fix):
1. **PR #259 title is false advertising** — claims to implement ui-033 diagnostic rules but doesn't
2. **Scope creep** — two unrelated fixes bundled under a feature ticket
3. **Missing changeset** — package changes won't be versioned correctly
4. **Ticket status incorrect** — ui-033 shows as complete when diagnostic rules were never merged

### What Went Right:
- High-quality tests for both fixes
- Atomic commits with good messages
- Bot identity used correctly
- No security issues

### Root Cause Hypothesis (without session transcript):
This looks like **improper branch selection during development**. Possible scenarios:
1. Agent was working on ui-033, discovered critical bugs, fixed them in-place on the same branch
2. Agent was told to "merge these fixes into ui-033 branch" without checking scope
3. Git branch confusion — pushed to wrong remote branch

Without the session transcript, I cannot determine which scenario occurred.

---

## Recommendations

### Immediate (Before Next PR):
1. **Rename/close this PR** — it doesn't match its title
2. **Update ticket ui-033** — mark as not started, since diagnostic rules aren't in main
3. **Create retroactive PRs** (if needed):
   - Extract infra-002 work into its own PR record
   - Document the JSX bug fix with a proper ticket
4. **Add missing changeset** for the package changes
5. **Review session transcript** (if available) to understand why scope was violated

### Process Improvements:
1. **Add a PR template** that requires:
   - Ticket reference (must match branch name)
   - Scope verification ("Does this PR fully implement the ticket?")
   - Changeset verification checklist
2. **Add a pre-PR check** script that verifies:
   - Branch name matches ticket in commit messages
   - Changeset exists if packages were modified
3. **Agent training:** Reinforce "Stay in scope" rule with concrete examples

---

## Evidence Summary

| Category | Grade | Key Issues |
|----------|-------|------------|
| TDD | ⚠️ | Cannot verify order without transcript; tests exist and are high-quality |
| Process | ❌ | Missing changeset, scope violation, branch mismatch |
| Design | ❌ | PR doesn't implement ticket ui-033; unrelated work bundled together |
| DX | ⚠️ | Good tests, but misleading PR title creates confusion |
| Security | ✅ | No issues |
| Git | ⚠️ | Good commit hygiene, but wrong branch and missing changeset |

**Overall:** D — Work is functional, but process violations are severe enough to warrant rework.

---

## Appendix: What PR #259 Should Have Been

If this were three separate PRs:

**PR A: fix(infra): quality gates in pre-push hook (infra-002)**
- Branch: `fix/infra-002-prepush-hook`
- Scope: `lefthook.yml`, test file
- Changeset: patch for `@vertz/repo` (or skip if infra-only)
- Grade: A

**PR B: fix(ui-compiler): JSX in conditional branches**
- Branch: `fix/ui-compiler-conditional-jsx`
- Scope: `packages/ui-compiler` and `packages/ui`
- Changeset: patch for both packages
- Grade: B (missing ticket, but solid bug fix)

**PR C: feat(ui-compiler): diagnostic rules (ui-033)**
- Branch: `feat/ui-033-compiler-diagnostics`
- Scope: Implement 5 diagnostic rules from commit d800177
- Changeset: minor for `@vertz/ui-compiler`
- Status: **Not yet created** (work exists but never merged)
- Expected Grade: A (if transcript shows TDD compliance)

The current PR #259 is a confusing mix of PR A and PR B under the title of PR C.
