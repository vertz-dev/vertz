# Local development environment variables
# Copy this file to .env and adjust as needed:
#   cp .env.example .env
#
# These defaults match the docker-compose.yml Postgres service.
# If you're running Postgres a different way, update the URLs accordingly.

# Development database — used by examples/task-api and manual testing
DATABASE_URL=postgres://postgres:postgres@localhost:5432/vertz_dev

# Test database — used by integration tests (packages/db)
# Wiped between test runs to ensure isolation
DATABASE_TEST_URL=postgres://postgres:postgres@localhost:5432/vertz_test
