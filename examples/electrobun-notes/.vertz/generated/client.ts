// Generated by @vertz/codegen — do not edit


import { FetchClient, type OptimisticHandler } from '@vertz/fetch';
import { createOptimisticHandler, getEntityStore } from '@vertz/ui';
import { createNotesSdk } from './entities/notes';

export interface ClientOptions {
  baseURL?: string;
  headers?: Record<string, string>;
  timeoutMs?: number;
  optimistic?: OptimisticHandler | false;
}

export function createClient(options: ClientOptions = {}) {
  const client = new FetchClient({ baseURL: options.baseURL ?? '/api', headers: options.headers, timeoutMs: options.timeoutMs });
  const optimistic = options.optimistic !== false ? (options.optimistic ?? createOptimisticHandler(getEntityStore())) : undefined;
  return {
    notes: createNotesSdk(client, optimistic),
  };
}

export type Client = ReturnType<typeof createClient>;