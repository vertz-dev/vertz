name: Task Manager CI

on:
  pull_request:
    paths:
      - 'examples/task-manager/**'
      - 'packages/ui-server/**'
      - 'packages/ui/**'
      - 'packages/core/**'
    branches:
      - main

  push:
    branches:
      - main

jobs:
  preview:
    name: Deploy Preview
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Install Dependencies
        run: bun install --frozen-lockfile

      - name: Build Workspace Dependencies
        run: turbo run build --filter='@vertz-examples/task-manager^...' --filter='!@vertz/cli' --output-logs=errors-only

      - name: Build Task Manager Worker
        run: bun run build:worker
        working-directory: examples/task-manager

      - name: Deploy to Cloudflare Workers (Preview)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: examples/task-manager
          command: |
            wrangler deploy --env preview --name vertz-task-manager-pr-${{ github.event.pull_request.number }}

      - name: Comment PR with Preview URL
        uses: actions/github-script@v7
        with:
          script: |
            const previewUrl = 'https://pr-${{ github.event.pull_request.number }}-task-manager.viniciusldacal.workers.dev';
            const commentBody = `🚀 **Preview Deployment Ready**\n\nThe task manager has been deployed to: ${previewUrl}\n\n- This preview will be available for the duration of this PR\n- Production deployment to follow`;
            
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            
            const existingComment = comments.find(comment => 
              comment.body.includes('Preview Deployment Ready')
            );
            
            if (existingComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existingComment.id,
                body: commentBody,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: commentBody,
              });
            }

  production:
    name: Deploy Production
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Install Dependencies
        run: bun install --frozen-lockfile

      - name: Build Workspace Dependencies
        run: turbo run build --filter='@vertz-examples/task-manager^...' --filter='!@vertz/cli' --output-logs=errors-only

      - name: Build Task Manager Worker
        run: bun run build:worker
        working-directory: examples/task-manager

      - name: Deploy to Cloudflare Workers (Production)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: examples/task-manager
          command: |
            wrangler deploy --env production --name vertz-task-manager
